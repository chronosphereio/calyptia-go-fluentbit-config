{
    "pipeline": {
        "parsers": [
            {
                "dummy_test": {
                    "name": "dummy_test",
                    "format": "regex",
                    "regex": "^(?<INT>[^ ]+) (?<FLOAT>[^ ]+) (?<BOOL>[^ ]+) (?<STRING>.+)$"
                }
            }
        ]
    }
}
